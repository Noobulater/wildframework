classDirector = {}

function classDirector.new( )
	local public = {}
	local nextEvent = 0 
	local cooldown = 5
	local thinkSpeed = 5 -- once every 5 seconds ( it doesnt need to be super influential on the game )
	
	function public.doEvent( eventName )

	end

	function public.think()
		if nextEvent <= CurTime() then
			
		end
	end

	return public
end